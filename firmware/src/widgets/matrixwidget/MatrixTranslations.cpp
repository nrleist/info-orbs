#include "MatrixTranslations.h"

// Languages are defined in I18n.h:
// EN, DE, FR

// All translation variables for MatrixWidget should start with "t_matrix"

constexpr Translation t_matrixBigFont = {
    "Big font (if checked)", // EN
};

constexpr Translation t_matrixTextColor = {
    "Text Color", // EN
};

constexpr Translation t_matrixHeadTextColor = {
    "Head Text Color", // EN
};

constexpr Translation t_matrixLineMin = {
    "Minimum line length", // EN
};

constexpr Translation t_matrixLineMax = {
    "Maximum line length", // EN
};

constexpr Translation t_matrixSpeedMin = {
    "Minimum line speed", // EN
};

constexpr Translation t_matrixSpeedMax = {
    "Minimum line speed", // EN
};

constexpr Translation t_matrixUpdateInterval = {
    "Screen Update Interval", // EN
};
